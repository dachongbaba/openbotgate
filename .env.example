# 可选：用于覆盖 openbotgate.yml / openbotgate.json 中的配置
# 建议将敏感信息（如 appSecret、token）放在 .env，不写入配置文件
# 复制本文件为 .env 后按需填写

# 网关与飞书（覆盖配置文件）
GATEWAY_TYPE=feishu
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_VERIFICATION_TOKEN=
FEISHU_DOMAIN=feishu

# 执行与输出（可选覆盖）
EXECUTION_TIMEOUT=120000
MAX_OUTPUT_LENGTH=10000
SHELL_OUTPUT_ENCODING=
CODE_TIMEOUT=

# 白名单与 Code 命令覆盖（可选，逗号分隔）
# ALLOWED_CODE_TOOLS=opencode,claudecode
# ALLOWED_SHELL_COMMANDS=git,dir,ls,pwd
# CODE_TOOL_COMMANDS=claudecode:claude.ps1
